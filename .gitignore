# Build output
/build/
*.nro
*.elf
*.nacp
*.map
*.orig
*.zip
/release_tmp/

# romfs/ is generated at build time (borealis resources merged from lib/).
# Regenerated by `make`; not tracked.
/romfs/

# Keys — never commit.
*.key
*.pem
/keys/
source/catalog/TinfoilPrivateKey.inc

# Exception: public key for server operators is safe to distribute
!tools/public_key.pem

# Encrypted index test artifacts
*.tfl

# Personal tools — not part of the project
encrypt.py
index.json
